#include <iostream>
// #include "globals.h"
#include "leadselfenergy.h"

using namespace negf;

int main()
{

  int i = 2;
  int m = 3;
  matrix h_ii = matrix::Random(i,i);
  matrix s_ii = matrix::Random(i,i);
  matrix h_im = matrix::Random(i,m);
  matrix s_im = matrix::Random(i,m);
  LeadSelfEnergy leadselfenergy(h_ii,s_ii,h_im,s_im,h_ii,s_ii);
  SelfEnergy selfenergy(h_ii,s_ii,h_im,s_im);
  GreenFunction *greenfunction;
  greenfunction = &selfenergy;
  std::cout << greenfunction->retarded(2.) << std::endl;
  std::cout << selfenergy.retarded(2.) << std::endl;
  std::cout << leadselfenergy.retarded(2.) << std::endl;
  // std::cout << selfenergy.Ginv << std::endl;
}
